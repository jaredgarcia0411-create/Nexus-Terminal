policy:
  allow_implicit_invocation: false

interface:
  display_name: "Nexus Commit"
  short_description: "Canonical local commit workflow for this repo"
  default_prompt: "Use $nexus-commit to review scope, validate the worktree, create a safe local commit, and push only when the user explicitly says push origin main."
