policy:
  allow_implicit_invocation: false

interface:
  display_name: "Review"
  short_description: "Spec compliance + ship-readiness review"
  default_prompt: "Use $nexus-review to compare the implementation against the active spec or requested scope, run validation, and report whether it is ready to ship."
