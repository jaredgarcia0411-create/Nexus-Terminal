policy:
  allow_implicit_invocation: false

interface:
  display_name: "Deep Research"
  short_description: "Repo-grounded research and review memos"
  default_prompt: "Use $nexus-deep-research to investigate a topic, delegate parallel passes only when useful, and save a structured markdown brief only if I ask for an artifact."
