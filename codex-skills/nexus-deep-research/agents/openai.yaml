policy:
  allow_implicit_invocation: false

interface:
  display_name: "Deep Research"
  short_description: "Repo-grounded research and review memos"
  default_prompt: "Use $nexus-deep-research to investigate a topic locally, use subagents only if I explicitly ask for delegation, and save a markdown brief only if I ask for an artifact."
