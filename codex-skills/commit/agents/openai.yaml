policy:
  allow_implicit_invocation: false

interface:
  display_name: "Commit"
  short_description: "Review, validate, commit, and push safely"
  default_prompt: "Use $commit to follow the Nexus Terminal commit workflow, validate changes, create a safe commit, and push when requested."
