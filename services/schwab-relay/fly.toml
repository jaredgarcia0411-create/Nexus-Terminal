app = "nexus-schwab-relay"
primary_region = "iad"

[build]
  [build.args]

[env]
  NODE_ENV = "production"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
  auto_stop_machines = false
  auto_start_machines = true
